# Railway: set "Root Directory" to this folder (TransportConnect/TransportConnect-)
# so the start command runs from the backend root. No "cd" needed.

[build]
builder = "nixpacks"

[deploy]
startCommand = "node backend/server.js"
