# 3D Models for Live Map

Place your Sketchfab GLB model here:

- **tracker.glb** – Used as the 3D tracking marker on the live map (Uber/Glovo-style).

The model will be scaled and rotated to match map coordinates. For best results, use a model that is roughly 1–2 meters in size (e.g., a vehicle or delivery icon).
